{% extends 'base_admin.html.twig' %}

{% block title %}Edit Program{% endblock %}

{% block body %}{{ parent() }}

    <div class="jumbotron">
        <h1 class="m-5 text-center">Modifier le programme</h1>
        <a href="{{ path('program_index') }}">back to list</a><br>
        <a href="{{ path('program_show', {'id': program.id}) }}">show</a>

    </div>

    {{ include('program/_form.html.twig', {'button_label': 'Update'}) }}

{% endblock %}
